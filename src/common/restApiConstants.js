const home = {
  url: '/testapi',
};

const dbpediaProxy = {
  url: '/dbpediaproxy/',
};

const testToken = {
  url: '/testapi/token/',
};

const spotifyToken = {
  url: '/api/token/',
};

module.exports = {
  home,
  dbpediaProxy,
  testToken,
  spotifyToken,
};
